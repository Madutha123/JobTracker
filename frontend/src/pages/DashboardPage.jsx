/**
 * Placeholder dashboard — will be fully implemented in the next run.
 */
export default function DashboardPage() {
  return (
    <div style={{
      minHeight: '100vh',
      background: 'var(--navy)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontFamily: 'var(--font-display)',
      color: 'var(--cream)',
      fontSize: '2rem',
      fontWeight: 700,
    }}>
      Dashboard — coming soon
    </div>
  );
}
